import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQ 연락처 관리",
};

export default function FaqContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
